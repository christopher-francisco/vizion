```
brew install xcode-build-server xcodegen xcbeautify ruby pipx
```

Fo

(Or if want to use tuist?)
```
brew tap tuist/tuist
brew install --formula tuist
tuist --generate-completion-script > ~/.oh-my-zsh/completions/_tuist
```

For xcodebuild.nvim
```
brew install xcbeautify ruby pipx
gem install xcodeproj
pipx install pymobiledevice3
```

- configure nvim-lspconfig with sourcekit
- install SwiftLint, SwiftFormat. Set them up with linter and formatter plugins

## Manually install the dap plugin things

https://wojciechkulik.pl/ios/the-complete-guide-to-ios-macos-development-in-neovim



